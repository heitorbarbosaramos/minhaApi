spring.output.ansi.enabled=ALWAYS

spring.profiles.active=${PROFILE:test}

server.servlet.context-path=/rest

# actuator
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*
management.info.env.enabled = true
info.app.name=@project.name@
info.app.description=@project.description@
info.app.version=@project.version@
info.app.encoding=@project.build.sourceEncoding@
info.app.java.version=@java.version@

# Configuracoes do JWT
config.jwt.secret=${JWT_SECRET:m1nh@P@l@vr4S3cr3t@}
config.jwt.expiration=${JWT_DAYS_EXPIRATION:1}

config.timezone.zone=${TIMEZONE_ZONE:America/Sao_Paulo}

# Configuracoes de email
spring.mail.username=${EMAIL_ORIGEM:vazio}
spring.mail.password=${EMAIL_SENHA:vazio}
spring.mail.host=${EMAIL_SMTP:vazio}
spring.mail.port=${EMAIL_PORT:vazio}
spring.mail.protocol=${EMAIL_PROTOCOLO:vazio}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.socketFactory.class = javax.net.ssl.SSLSocketFactory
